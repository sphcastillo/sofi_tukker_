generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


model StoreItem {
  id    Int    @id @default(autoincrement())
  title String
  image String
  price String

  @@map("store_items")
}