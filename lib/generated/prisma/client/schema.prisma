generator client {
  provider = "prisma-client-js"
  output   = "../lib/generated/prisma/client"
}

datasource db {
  provider = "postgresql"
}

model StoreItem {
  id    Int    @id @default(autoincrement())
  title String
  image String
  price String

  @@map("store_items")
}
